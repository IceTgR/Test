cd ORDNER # go into repo/Ordnerumgebung auf PC
git status # check what changed
git add . # alle Änderungen im Repository lokal hinzufügen
git commit -m "description" # Kurzbeschreibung was geändert
git push origin main # push to GitHub (HOCHLADEN)
git pull # hol mir die aktuelle Version

git clone (LINK VON GITHUB)

cd = change directory

Tschüssi